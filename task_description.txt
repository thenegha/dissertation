The program should take a list of whole numbers as input, and the list may contain repeated numbers. It should show a sorted version of that list in ascending order with duplicates removed, and the student is still deciding whether to also show the original list alongside it.
The program should take a list of whole numbers that may include repeats and show a sorted version with the duplicates removed. The student is still undecided about whether it should also show the original list alongside the cleaned-up sorted list for context.
The program should ask the user for a list of whole numbers that may include duplicates. It should sort the numbers, remove repeated values so only one copy of each number remains, and show the final cleaned, sorted list.
The program should ask for a list of whole numbers, then sort the numbers and remove duplicates so only one of each number remains. It should display the cleaned, sorted list, or display "nothing to sort" if the input list is empty.
